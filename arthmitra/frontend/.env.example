# Development (recommended): leave unset — Vite dev server proxies /auth + /api to FastAPI.
# Production / custom: set full API origin, e.g. https://api.yourdomain.com
# Direct local (no proxy): VITE_API_URL=http://127.0.0.1:8000

