# Local dev: leave VITE_API_URL unset → axios uses same origin + Vite proxies /auth and /api → FastAPI.
# Only set this if you run the built SPA from file:// or a host not in vite.config proxy:
# VITE_API_URL=http://127.0.0.1:8000
